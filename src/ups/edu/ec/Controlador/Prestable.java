package ups.edu.ec.Controlador;

// Interfaz que define métodos para prestar y devolver objetos.
public interface Prestable {
    
    // Método para marcar un objeto como prestado.
    public void prestar();
    
    // Método para marcar un objeto como devuelto.
    public void devolver();
}
